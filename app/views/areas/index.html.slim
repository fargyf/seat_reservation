h1 Listing areas

table
  tr
    th Name
    th Description
    th X_max
    th Y_max
    th seats
    th
    th
    th

  - @areas.each do |area|
    tr
      td = area.name
      td = area.description
      td = area.x_max
      td = area.y_max
      td = link_to area.seats.size, map_seats_path(:area_id => area.id)
      td = link_to 'Show', area
      td = link_to 'Edit', edit_area_path(area)
      td = link_to 'Destroy', area, :confirm => 'Are you sure?', :method => :delete

br

= link_to 'New Area', new_area_path
